{
  "name": "@jakedeichert/db-utils",
  "version": "0.1.0",
  "author": "Jake Deichert",
  "private": true,
  "main": "dist/index.js",
  "scripts": {
    "dev": "npm run clean && tsc --watch",
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist"
  }
}
