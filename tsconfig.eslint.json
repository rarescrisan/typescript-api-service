// Required for typescript-eslint to properly check all test files.
// The normal tsconfig.json "excludes" tests so typescript-eslint
// was ignoring them.
{
  "exclude": ["node_modules", "dist"],
  "extends": "./tsconfig.base.json"
}
